apiVersion: v1
kind: Service
metadata:
  name: postgres
spec:
  selector:
    app: postgres-db
  ports:
    - port: {{ .Values.dbService.ports.port }}
      targetPort: 5432
      protocol: TCP
  selector:  
    app: postgres-db