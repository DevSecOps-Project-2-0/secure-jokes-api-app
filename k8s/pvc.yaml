apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-pvc 
  labels:
    app: postgres-db
spec:
  storageClassName: standard
  accessModes:
  - ReadWriteOnce
  resources:
    requests: 
      storage: 2Gi